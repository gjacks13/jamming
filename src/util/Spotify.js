let accessToken = "";
const clientId = "";
const redirectUri = "http://localhost:3000/";

const Spotify = {
  getAccessToken: function() {
    if (accessToken) {
      return accessToken;
    } else {

    }
  }
};

export default Spotify;